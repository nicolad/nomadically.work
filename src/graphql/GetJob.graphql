query GetJob($id: String!) {
  job(id: $id) {
    id
    external_id
    source_id
    source_kind
    company_key
    title
    location
    url
    description
    posted_at
    score
    score_reason
    status
    created_at
    updated_at
  }
}
