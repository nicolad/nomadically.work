query GetJobs(
  $sourceType: String
  $search: String
  $limit: Int
  $offset: Int
  $excludedCompanies: [String!]
) {
  jobs(
    sourceType: $sourceType
    search: $search
    limit: $limit
    offset: $offset
    excludedCompanies: $excludedCompanies
  ) {
    jobs {
      id
      external_id
      source_kind
      company_key
      title
      location
      url
      posted_at
      skills {
        tag
        level
      }
    }
    totalCount
  }
}
