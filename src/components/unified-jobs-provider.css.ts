import { style } from "@vanilla-extract/css";

export const pageHeading = style({
  color: "var(--gray-12)",
  letterSpacing: "-0.02em",
});

export const pageSubheading = style({
  color: "var(--gray-11)",
});
