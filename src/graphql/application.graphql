# Application-related GraphQL mutations and queries

mutation CreateApplication($input: ApplicationInput!) {
  createApplication(input: $input) {
    email
    jobId
    questions {
      questionId
      questionText
      answerText
    }
  }
}
